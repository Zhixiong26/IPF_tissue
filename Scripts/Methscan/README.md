# Methscan

本阶段保存经确认的本地 methscan 流程及其调度入口。 / This stage contains the confirmed local methscan workflow and scheduler entry points.

添加命令前，先记录已安装的可执行文件/版本和预期生物学操作；记录准确输入、输出、参考基因组、参数、断点行为及脚本顺序。结果写入 `Results/Methscan/`，日志写入 `Scripts/Methscan/logs/`。

Before adding a command, record the executable/version and intended biological operation. Document exact inputs, outputs, reference build, parameters, restart behavior, and script order. Write results below `Results/Methscan/` and logs below `Scripts/Methscan/logs/`.

尚未确认或登记 methscan 命令。 / No methscan command has been confirmed or registered yet.
