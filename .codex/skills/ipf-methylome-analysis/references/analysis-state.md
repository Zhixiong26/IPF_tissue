# IPF iterative analysis state

Last updated: 2026-08-25

## Confirmed observations

- Skill collaboration rule: for each new project, remind the user to establish and provide a GitHub SSH repository URL and default branch, then verify SSH readiness without requesting any private key, token, or password. Repository creation, remote changes, commits, and pushes remain explicit user-authorized actions.
- Skill intake rule: after initialization, ask whether the analysis needs FASTQ. FASTQ may be skipped; require a Raw FASTQ path only when selected, otherwise require the downstream starting data type, path, and known provenance before validation.
- Corrected project structure: logs are stage-specific at `Scripts/<stage>/logs/`, not a shared project-root destination. Root-level `log/` and `logs/` are retained only as legacy directories and are no longer created or used by the layout helper.
- Matching `FastQ/`, `Scanpy/`, `Methscan/`, and `Methylvi/` stage directories were added under both `Scripts/` and `Results/`. Existing root-level scripts and historical MethylVI result roots were not moved.
- Every stage directory under `Scripts/` now requires `README.md` and `Report.md`; initialization is non-destructive and future stages receive both documents automatically.
- Project documentation convention: stage `README.md` and `Report.md` files are maintained bilingually in Chinese and English until the user requests otherwise.
- Six source directories are linked under `Data/Raw_fastq`: three in `E/` and their three counterparts in `Met/`.
- The legacy/current downstream workflow begins with Bismark six-column coverage files (`Data/30wcov`) and already provides ALLCools, Scanpy plotting, and MethylVI scripts under `Scripts/`.
- The FASTQs sit one level below each linked batch in subsets `1/` and `2/`. All 24 observed FASTQs follow the paired-end `*_R1_001.fastq.gz` / `*_R2_001.fastq.gz` pattern (12 candidate pairs).
- Raw FASTQ validation passed on 2026-08-24: 6 non-broken source links, 24 readable non-empty files, 12 complete R1/R2 pairs, valid gzip decoding and FASTQ syntax for the first record of every file, and matching first-record names within every pair. Total compressed size is 1,663,551,234,393 bytes (E: 139,446,025,270; Met: 1,524,105,209,123).
- Validation was intentionally lightweight: it did not sequentially decompress all 1.66 TB, so full-stream gzip integrity and whole-file checksums remain untested.
- The subset directory is not yet interpreted as a biological replicate, sequencing lane, or technical partition; a sample sheet must retain it explicitly until confirmed.
- GitHub `origin` and branch are configured. GitHub authentication succeeds with explicit key `~/.ssh/id_ed25519_github`, but default `ssh -T git@github.com` fails because no matching SSH `IdentityFile` configuration was observed. No SSH configuration was changed.
- No Bismark reference choice, library protocol, or methscan command has yet been recorded for the newly linked data.
- The user explicitly chose to temporarily skip FASTQ merging/processing and start from linked BAM data at `Data/Bam`. The six BAM batch directories are in scope; their files, indexes, headers, reference dictionary, sort order, read groups, and sample mapping have not yet been validated.
- Environment policy now supports either a shared compatible environment or separate per-stage environments. Existing environments are discovered and smoke-tested before any user-approved creation or installation.
- Read-only environment discovery completed. The current hybrid mapping is stored in `Supplementary/environments.tsv`: BAM and Scanpy/ALLCools share `allcools`; Methscan uses `MethSCAn`; MethylVI uses the user's `methylvi`; the `moabs` Bismark candidate is deferred with Samtools 0.1.19 excluded from BAM validation.
- A dedicated bilingual `QC` stage now implements six non-destructive per-cell steps: FASTQ pair counting, CB-aware BAM mapping counts, FASTQ/BAM merge, ALLC methylation metrics, threshold flags, and per-sample summaries.
- Representative CYL Bismark BAM inspection confirmed 17-bp `CB:Z` tags. A 1,000-primary-record smoke scan found 0 records missing CB, 500 read1 and 500 read2 records, and 108 cell barcodes.
- The Met FASTQ barcode structure is now verified: R1 bases 1–17 are the cell barcode, R1 bases 18–29 are the UMI, and the 8-bp header suffix (for example `TTCAACGG`) is the sample index. Across CYL, LC_S9_1N, and ZCP, 50 read-name-matched raw/BAM records per sample gave 150/150 exact CB matches and 150/150 exact UMI matches.
- In 10,000 raw R1 reads from every Met partition, all but one of 60,000 reads had an A/C/G/T-only 17-bp prefix. For CYL and ZCP, 47–68% of these reads matched the existing post-selection ALLC cell set; LC has no corresponding ALLCs in that directory, so absence there is not evidence against the barcode rule.
- Step 01 now defaults to `R1[0:17]`, rejects barcodes containing non-ACGT bases, accepts the step-02 `sample_id/barcode` TSV directly as its whitelist, and reports invalid versus non-whitelisted pairs separately.
- Representative existing ALLC files contain only `CGN`, consistent with their conversion from six-column Bismark CpG coverage. These files support mCG and covered CpG counts but not mCH/mCCC; QC scripts emit `NA` for absent contexts rather than zero.
- Server resource snapshot at 2026-08-24 14:23 CST: login node `ctl01` has 32 physical CPU cores and 125 GiB RAM (about 104 GiB available). Slurm provides `cpu` with 3 × 56-core/~257 GiB nodes and `fat` with 1 × 192-core/~1.03 TiB node; `cu02`/`cu03` were idle and `fat01` had about 913 GiB free memory. No GPU resource was observed and the user had no queued/running jobs.
- Storage is the immediate constraint: `/home/lijia/luozhixiong` is 100% used with only about 1 GiB free and 96% inode usage, while `/mnt/data04` is 96% used with about 4.7 TiB free. Do not launch a full QC run that writes large intermediates beneath the project root until output placement is resolved.
- Later resource recheck on 2026-08-24: the 16 GiB figure is free swap, not RAM; login-node available RAM is about 108 GiB. `/home/lijia/luozhixiong` free space increased to about 40 GiB and inode usage fell/reported at about 35%, though `df` still rounds usage to 100%. The stream-based QC outputs are small enough to run, while large intermediates should still use another filesystem.
- QC scan-heavy scripts use bounded multiprocessing: step 01 across the two FASTQ partitions (default 2 workers), step 02 across 156 prefix BAMs per sample (default 16), and step 04 across per-cell ALLCs (default 16). Small table joins remain serial. Serial/parallel smoke comparisons were byte-identical for all three metric layers.
- BAM volume is CYL 0.537 TiB, LC_S9_1N 0.498 TiB, and ZCP 0.632 TiB. The preferred non-submitted launchers are separate arrays: `07_run_parallel_bam.sbatch` requests 3 × 16 CPUs/32 GiB and `08_run_parallel_fastq.sbatch` requests 3 × 2 CPUs/8 GiB with an `aftercorr` dependency. Both require an explicit output root and refuse overwrite.
- QC array jobs 307475 (BAM) and 307476 (dependent FASTQ) were subsequently submitted with an output path under `/mnt/data04`; the fat compute node could not create/access that path (`mkdir: cannot create directory '/mnt/data04': Permission denied`). Those jobs do not constitute a successful QC run.
- Compute-node path rule established after that failure: the fat node cannot access `/mnt/data04`, including project symlinks resolving there. Inputs needed by compute jobs must be staged to compute-readable local project storage after checking capacity. Derived stage outputs belong under the matching `Results/<stage>/` root unless a stage-specific user decision says otherwise.
- Current biological scope for the transcriptome-E analysis is CYL and ZCP only. LC is not copied or included in this Scanpy analysis, even if older QC launchers remain preconfigured for three groups.
- Compute-readable 10x matrices are present at `Data/Matrix/25100718_CYL_E/filtered_feature_bc_matrix.zip` and `Data/Matrix/25100718_ZCP_E/filtered_feature_bc_matrix.zip`.
- The 2026-08-24 local fat-node staging task finished at 2026-08-25 01:30:58 +0800 with `ALL_COPY_AND_VERIFY_COMPLETE` and `.copy_complete`. Read-only rsync dry-runs on 2026-08-25 found no source/target differences for CYL/ZCP E and Met BAMs or FASTQs; both staged CYL/ZCP matrix ZIPs are byte-identical to `Data/Matrix/` and pass ZIP integrity checks. The staged BAM/FASTQ payload contains 644 files and 2,405,326,872,673 bytes. A separate 734,003,200-byte file remains under `.orphaned_diagnostic/` and is not part of the verified payload.
- The validated notebook is `Scripts/Scanpy/Notebooks/E_CYL_ZCP_scanpy.ipynb`. Its confirmed workflow performs per-cohort QC/filtering and Scrublet, merges singlets, normalizes/log-transforms, selects cohort-aware Seurat HVGs, runs PCA and Harmony, builds Harmony neighbors, calculates UMAP/Leiden, ranks markers, and applies reviewed manual annotation with a cluster-ID guard.
- Confirmed notebook defaults include min genes 200, max genes 6000, min counts 500, MT% below 5, gene min-cells 3, dynamic Scrublet rate `0.004 * n_cells / 1000`, 2,000 HVGs, 50 PCA components, 30 Harmony PCs for neighbors, 15 neighbors, and Leiden resolution 0.8. The complete parameter ledger is maintained in `Scripts/Scanpy/Report.md` rather than duplicated here.
- The latest reviewed result has 18 Leiden clusters (0–17) at resolution 0.8. Primary labels are AT2, two secretory epithelial classes, fibroblasts, ciliated cells, macrophages, AT1-like, endothelial cells, basal cells, mural cells, MT-high AT2-like, T cells, cycling cells, lymphatic endothelial cells, and `NA` for clusters 16/17. The mapping is invalid if a fresh run produces a different cluster set.
- Validation counts are 111 cells for cluster 14, 110 for cluster 15, 50 for cluster 16, and 20 for cluster 17. Cluster 15 expresses PROX1/FLT4/LYVE1/CCL21/MMRN1/RELN in about 29/40/22/19/59/49% of cells with no DCN detection, supporting lymphatic endothelial identity. Cluster 14 retains `Cycling cells`; clusters 16 and 17 are low-confidence, sample-skewed populations (94% CYL and 90% ZCP) whose primary cell type is now user-specified `NA`, with epithelial-like/stromal-like clues retained only as evidence notes.
- The production conversion is `Scripts/Scanpy/Scripts/01_run_e_scanpy.py` with `Scripts/Scanpy/Scripts/run_e_scanpy.sbatch`. It exports staged QC/doublet/HVG/PCA/cluster/marker/annotation tables, figures, runtime parameters/versions, and a final or unreviewed H5AD beneath `Results/Scanpy/`. Its audit interface now includes an always-written annotation guard, a joined per-cluster validation table, merged cell-type membership, and an ordered dotplot-marker manifest.
- `Scripts/Scanpy/Scripts/02_validate_annotation_config.py` provides a matrix-free preflight for reviewed cluster IDs, confidence tiers, merged labels, focus/rare subsets, and the three-to-four-marker dotplot contract. Biological validation remains dependent on run-derived marker/QC evidence.
- The Scanpy production script and Slurm wrapper passed Python syntax/import/interface checks and `bash -n`; no smoke or formal Scanpy job has yet been executed. A fresh rerun is expected to verify whether the reviewed 0–17 cluster set is reproducible before annotation can be marked final.
- The notebook and production script now retain matched UMAPs before Harmony (`X_pca`) and after Harmony (`X_pca_harmony`) for direct sample-colored comparison. Final Harmony-coordinate plots are separate for cell type and sample. A third group plot is conditional because no verified group metadata currently exists; sample is not reused as group.
- Annotation dotplots merge clusters by reviewed cell type before aggregation. The y-axis and top marker groups use the same cell-type set and dendrogram-synchronized order, with three to four markers per type, red raw log-normalized mean expression, fraction-sized dots, and a right-side Pearson/complete-linkage dendrogram.
- A user-supplied prior broad annotation of the same CYL/ZCP tissue agrees with the major AT2, AT1-direction, ciliated, basal, secretory, macrophage/myeloid, endothelial, mural, fibroblast, and proliferating lineages. It is auxiliary evidence only because no prior per-cell barcode labels are available. The maintained lineage crosswalk is `Supplementary/prior_annotation_crosswalk.tsv`.
- Extended cluster-6 evidence supports `AT1-like`, not pure AT1: CAV1/HOPX/CAV2 are detected in about 65/63/36% of cells, whereas AGER/PDPN/AQP5 are about 15/11/3%, and SFTPB/LPCAT1/ABCA3/SFTPC remain about 96/73/49/33%. Prior NA does not override the current T-cell (13) or lymphatic-endothelial (15) marker evidence.

## Current next action

Run an end-to-end Scanpy smoke/formal preflight using the compute-readable CYL/ZCP matrix ZIPs and a new empty root under `Results/Scanpy/`. Do not submit until the user requests execution. After the run, verify the result summary, cluster set, marker evidence, annotation guard, H5AD schema, figures, and tables; update the Scanpy Report and this state file with the command, job ID, counts, result path, and annotation decision. The failed QC submission and full-context methylation recovery remain separate pending work.

## Run ledger

| Date | Stage | Command/job | Outcome | Notes |
|---|---|---|---|---|
| 2026-08-24 | Setup | Created project-local `ipf-methylome-analysis` skill | complete | Initial reusable workflow and state tracking established. |
| 2026-08-24 | Project layout | `scripts/ensure_project_layout.sh` | complete | Required `Scripts/`, `Results/`, `logs/`, and `Supplementary/` contract established; legacy `log/` preserved. |
| 2026-08-24 | Log layout correction | Updated layout helper and documentation | complete | New logs now go to `Scripts/<stage>/logs/`; root `log/` and `logs/` are legacy only. |
| 2026-08-24 | Stage layout | `scripts/ensure_project_layout.sh` | complete | Added matching `FastQ`, `Scanpy`, `Methscan`, and `Methylvi` directories under `Scripts` and `Results`; helper supports additional stage names. |
| 2026-08-24 | Stage documentation | `scripts/init_stage_docs.py` | complete | Added non-overwriting README/Report creation for every Scripts stage. |
| 2026-08-24 | Bilingual documentation | Updated stage docs and templates | complete | Stage README/Report content is maintained in aligned Chinese and English. |
| 2026-08-24 | Starting-point intake rule | Updated `SKILL.md` | complete | Initialization now asks whether FASTQ is needed; users may skip it and provide a downstream starting dataset. |
| 2026-08-24 | GitHub/SSH intake rule | Updated skill and added `references/github-ssh.md` | complete | Future projects prompt for SSH repository metadata and safe SSH setup; secrets are prohibited. |
| 2026-08-24 | FASTQ inventory | Read-only directory inspection | complete | 6 batches × 2 subsets × paired-end R1/R2 = 24 FASTQs / 12 candidate pairs. |
| 2026-08-24 | GitHub/SSH gate | `git status`, `git remote -v`, `ssh -T` | conditional pass | Remote/branch present; explicit GitHub key authenticates, but default SSH key selection is not configured. No remote, commit, push, or SSH config mutation performed. |
| 2026-08-24 | Raw FASTQ validation | `Scripts/FastQ/01_validate_raw_fastq.py` | pass | Lightweight validation passed for 24 files/12 pairs; complete gzip-stream integrity not tested. |
| 2026-08-24 | CYL E R1 merge preflight | Source size and filesystem check | blocked | Required 21,723,176,838 bytes; target filesystem had about 6.2 GB free. Merge not started and no partial file created. |
| 2026-08-24 | Starting-point override | User selected `Data/Bam` | active | FASTQ merging/processing deferred; next stage is BAM inventory and validation. |
| 2026-08-24 | Environment stage | Added discovery workflow and shared/per-stage policy | active | Read-only discovery precedes any user-approved environment creation or installation. |
| 2026-08-24 | Environment discovery | `Scripts/Environment/01_discover_environments.sh` plus smoke checks | complete | Hybrid mapping recorded; no environment mutation performed. |
| 2026-08-24 | Per-cell QC implementation | `Scripts/QC/01_FASTQ_per_cell_reads.py` through `06_QC_summary.py` | complete | Layered tables and non-destructive QC flags implemented with strict configurable thresholds. |
| 2026-08-24 | QC smoke test | `Results/QC/smoke_20260824/` | pass | Two synthetic cells exercised all six steps; expected one primary pass and one mCG/mCH/mCCC failure. |
| 2026-08-24 | BAM CB smoke scan | CYL AAAG BAM, first 1,000 primary mapped records | pass | 1,000/1,000 records carried CB; 500 read1, 500 read2, 108 observed barcodes. |
| 2026-08-24 | Met barcode-rule validation | Raw R1 versus BAM qname/CB/UR cross-check | pass | Confirmed R1[0:17]=CB and R1[17:29]=UMI with 50/50 exact matches in each of CYL, LC_S9_1N, and ZCP. |
| 2026-08-24 | FASTQ whitelist smoke test | CYL partition 2, first 100,000 pairs with partial BAM whitelist | pass | 1,196 pairs assigned to 80/108 partial-whitelist cells; 0 structurally invalid barcodes. |
| 2026-08-24 | Project TODO | Added `Supplementary/TODO.md` | active | Full-context ALLC/mCH/mCCC recovery recorded as a separate pending task. |
| 2026-08-24 | Server resource inventory | `lscpu`, `free`, `df`, `sinfo`, `squeue` | complete | CPU/fat Slurm capacity is available; project filesystem is effectively full and is the current execution constraint. |
| 2026-08-24 | Parallel QC implementation | Steps 01, 02, 04 plus separate BAM/FASTQ arrays | complete | Serial/parallel outputs were identical; final allocation is 16 workers/sample for BAM, 2/sample for FASTQ, and 16 for ALLC. No Slurm job submitted. |
| 2026-08-24 | QC submission preflight | Input counts, output root, syntax, executables, `sbatch --test-only` | pass | Planned root is new/writable; both arrays accepted by Slurm. Test IDs are not jobs; production submission awaits approval. |
| 2026-08-24 | QC production submission | BAM job 307475; dependent FASTQ job 307476 | failed | Fat node could not access/create `/mnt/data04`; no successful production QC output was established. |
| 2026-08-25 | Transcriptome input staging | CYL/ZCP 10x matrix ZIPs under `Data/Matrix/` | complete | Local compute-readable matrices are present; LC is outside the current transcriptome scope. |
| 2026-08-25 | Fat-node input staging verification | `Data/.fat_stage_20260824/` logs, rsync dry-runs, ZIP/cmp checks | pass | Copy completed at 01:30:58; CYL/ZCP E/Met BAM and FASTQ trees match their sources and both matrix ZIPs match. No copy process remains active. |
| 2026-08-25 | Scanpy notebook validation | `Scripts/Scanpy/Notebooks/E_CYL_ZCP_scanpy.ipynb` | complete with guard | QC/Scrublet/Harmony/Leiden/marker workflow and 18-cluster annotation reviewed; mapping must not transfer to changed cluster IDs. |
| 2026-08-25 | Scanpy 18-cluster review | Existing marker/QC validation tables | reviewed with provisional tiers | Added focused epithelial and rare-cluster audits; cluster 15 supported as lymphatic endothelial, while 14/16/17 retain review or low-confidence status. |
| 2026-08-25 | Scanpy production conversion | `01_run_e_scanpy.py`, `run_e_scanpy.sbatch`, README/Report | implementation complete | Results route to `Results/Scanpy/`; syntax/import/CLI and shell checks pass; end-to-end run not yet executed. |
| 2026-08-25 | Skill update | Added transcriptome Scanpy reference and refreshed project state | complete | Skill now distinguishes RNA Scanpy from methylome ALLCools and enforces compute-path and annotation invariants. |
| 2026-08-25 | Scanpy annotation operationalization | Cluster-validation skill reference, Python validator, and audit exports | static/synthetic checks pass | Codified 0–17 evidence/correction rules; validator, Python compile/CLI, wrapper syntax, skill package, and synthetic joined-table tests pass. End-to-end expression run remains pending. |
